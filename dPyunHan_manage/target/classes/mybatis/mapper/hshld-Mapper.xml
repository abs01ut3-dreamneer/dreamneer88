<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.mapper.HshldMapper">
    <select id="findActiveResidents" parameterType="hshldVO" resultType="hshldVO">
        SELECT HSHLD_ID, ADRES, APTCMPL, HO, PRVUSE_AR, SUPLY_AR, CMMNTY_USE_AT, RESIDESTTUS, MVN_DE, LVHS_DE
        FROM   HSHLD
        WHERE  RESIDESTTUS = 1
    </select>

    <select id="getAllHouseholdIds" resultType="string">
        SELECT HSHLD_ID
        FROM HSHLD
        WHERE  RESIDESTTUS = 1
    </select>
</mapper>